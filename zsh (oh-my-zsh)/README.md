Run the following command to change your shell to Zsh:

```bash
chsh -s $(which zsh)
```

To customize Zsh with themes and plugins, install the Oh My Zsh framework: 

```bash
sh -c "$(curl -fsSL https://raw.githubusercontent.com/ohmyzsh/ohmyzsh/master/tools/install.sh)"
```